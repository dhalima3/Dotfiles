# Nerd Font glyphs cover the Powerlevel10k prompt.
font-family = FiraCode Nerd Font Mono

# Ghostty bundles this theme.
theme = iTerm2 Solarized Dark

# Preserve windows, tabs, splits, and working directories across launches.
window-save-state = always
